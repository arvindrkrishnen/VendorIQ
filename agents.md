# Agents Index

The executable agent definitions are maintained in the `agents/` folder so each agent can be independently updated.

Start with:

- `agents/agent_registry.md`
- `agents/orchestrator_agent.md`

Online models should read `agent_registry.md` first, then load specific agent files as needed for each report section.
