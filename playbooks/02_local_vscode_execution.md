# Playbook 02: Local VS Code Execution

Run `src/universal_vendor_orchestrator.py` with a configured provider.

```bash
python src/universal_vendor_orchestrator.py --vendor "Rubrik, Inc." --ticker RBRK --provider perplexity --model sonar-pro
```

The only persistent output must be `artifacts/exhaustive_final_report.md`.
